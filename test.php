<head>
    <!-- some comm html
    -->
    <title>My page</title>
    <!-- anduin -->
</head>
<?php
//////////////////////////////////////////////////////////////// ($readString = fgets($openedFile, 8192)) !== false
$start = "this char for my didn't \"so so do"; // and so it's begin
echo $start;
/*
if (($checkup = mb_strpos($writeString, '<?')) !== false) {
    $registers['php_flags']['code'] = true;
    fwrite($madeFile, mb_substr($writeString, 0, $checkup+2) . PHP_EOL);
    $writeString = mb_substr($writeString, $checkup+2);

} elseif (($checkup = mb_strpos($writeString, '<!--')) !== false) {
    $registers['html_com_flag'] = true;
    fwrite($madeFile, mb_substr($writeString, 0, $checkup+4) . PHP_EOL);
    $writeString = mb_substr($writeString, $checkup+4);
} else {
    fwrite($madeFile, $writeString);
    $writeString = '';
}
*/
echo 54;
